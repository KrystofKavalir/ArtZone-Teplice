<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="shortcut icon" href="/Img/logo_v2_icon.ico" type="image/x-icon">
    <link rel="stylesheet" href="/Style/adminSettings.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <title>Administrace</title>
    <style>
        main {
            margin-top: 110px;
            margin-bottom: 40px;
            margin-left: auto;
            margin-right: auto;
            max-width: 900px;
            padding: 0 10px;
        }
        @media (max-width: 991.98px) {
            main {
                max-width: 100%;
                padding: 0 5px;
            }
        }
        .admin-card {
            border-radius: 1.5rem !important;
            box-shadow: 0 4px 24px rgba(0,0,0,0.08);
            background: #fff;
            margin-bottom: 2rem;
        }
        .admin-btn {
            border-radius: 1.2rem !important;
            font-weight: 600;
            font-size: 1.1rem;
            margin-bottom: 1rem;
            background: #f8f9fa;
            color: #222;
            border: 1px solid #d1d5db;
            transition: background 0.15s, color 0.15s, border 0.15s;
        }
        .admin-btn:hover, .admin-btn:focus {
            background: #e2e6ea;
            color: #111;
            border: 1px solid #bfc4c9;
        }
        .admin-btn-user {
            background: #e9ecef;
            color: #185a9d;
            border: 1px solid #d1d5db;
        }
        .admin-btn-user:hover, .admin-btn-user:focus {
            background: #dbe4ea;
            color: #185a9d;
            border: 1px solid #bfc4c9;
        }
        .admin-btn-authorize {
            background: #e9f7ef;
            color: #198754;
            border: 1px solid #d1e7dd;
        }
        .admin-btn-authorize:hover, .admin-btn-authorize:focus {
            background: #d1f2e2;
            color: #157347;
            border: 1px solid #bcd0c7;
        }
        .admin-section-title {
            font-weight: 700;
            letter-spacing: 1px;
            margin-bottom: 1.2rem;
        }
        .admin-list-group {
            border-radius: 1rem;
            overflow: hidden;
        }
        .admin-list-group-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 1.2rem;
            border-bottom: 1px solid #eee;
            background: #f9f9f9;
        }
        .admin-list-group-item:last-child {
            border-bottom: none;
        }
        /* Overlay styles */
        .overlay {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background-color: rgba(0, 0, 0, 0.8);
            display: none;
            z-index: 1000;
            overflow: auto;
        }
        .overlay-content {
            position: relative;
            top: 50%; left: 50%;
            transform: translate(-50%, -50%);
            background-color: #fff;
            padding: 2rem 1.5rem;
            border-radius: 1.2rem;
            width: 90%;
            max-width: 500px;
            text-align: center;
            box-shadow: 0 8px 32px rgba(0,0,0,0.13);
        }
        .overlay-content h2 {
            margin-bottom: 1.5rem;
        }
        .overlay .closebtn {
            position: absolute;
            top: 10px; right: 20px;
            font-size: 2rem;
            cursor: pointer;
            color: #333;
        }
        .event-list {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }
        .event-list li {
            margin-bottom: 10px;
        }
        .event-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
            padding: 0.7rem 1rem;
            border: 1px solid #ccc;
            border-radius: 0.7rem;
            background: #f9f9f9;
            cursor: pointer;
            transition: background-color 0.3s;
            margin-bottom: 0.5rem;
        }
        .event-item:hover {
            background-color: #ececec;
        }
        a {
            text-decoration: none;
        }
        @media (max-width: 768px) {
            .admin-card {
                padding: 1rem 0.5rem;
                border-radius: 1rem;
            }
            main {
                margin-top: 90px;
                margin-bottom: 20px;
                padding: 0 2px;
            }
        }
    </style>
</head>
<body>
    <header id="header">
        <a href="/" class="logo"><img class="logo" src="/Img/logo.png"></a>
        <nav class="navBar">
            <a href="/">Domů</a>
            <a href="/umelci">Umělci</a>
            <a href="/kalendar">Kalendář akcí</a>
            <a href="/kontakt">Kontakt</a>
        </nav>
        <span class="openbtn" onclick="openNav()">&#9776; Menu</span>
        <a href="/profil" class="profPic w3-display-right"><img class="profPic w3-circle" src="https://static.vecteezy.com/system/resources/previews/005/544/718/non_2x/profile-icon-design-free-vector.jpg" alt=""></a>
    </header>
    <div id="sidebar" class="sidebar">
        <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">&times;</a>
        <a href="/">Domů</a>
        <a href="/umelci">Umělci</a>
        <a href="/kalendar" class="active">Kalendář akcí</a>
        <a href="/kontakt">Kontakt</a>
    </div>
    <main>
        <h1 class="text-center mb-4 admin-section-title">Administrace</h1>
        <div class="row g-4">
            <div class="col-12 col-lg-6">
                <div class="card admin-card p-4">
                    <div class="d-grid gap-2 mb-4">
                        <a href="/adminAddAkce" class="btn admin-btn">
                            <i class="bi bi-plus-circle"></i> Přidat akci
                        </a>
                        <button type="button" class="btn admin-btn" onclick="openEventOverlay()">
                            <i class="bi bi-pencil-square"></i> Upravit akci
                        </button>
                    </div>
                    <h2 class="admin-section-title fs-5">Upravit uživatele</h2>
                    <form action="/adminEditUzivatel" method="get" class="form-edit-uzivatel">
                        <label for="userId" class="mb-1">Vyberte uživatele:</label>
                        <select id="userId" name="userId" class="form-select mb-2" required>
                            <% allUsers.forEach(user => { %>
                                <option value="<%= user.id %>"><%= user.jmeno %> (<%= user.email %>)</option>
                            <% }) %>
                        </select>
                        <button type="submit" class="btn admin-btn admin-btn-user w-100">
                            <i class="bi bi-person-lines-fill"></i> Upravit uživatele
                        </button>
                    </form>
                </div>
            </div>
            <div class="col-12 col-lg-6">
                <div class="card admin-card p-4">
                    <h2 class="admin-section-title fs-5">Čekající uživatelé</h2>
                    <ul class="list-group admin-list-group">
                        <% users.forEach(user => { %>
                            <li class="list-group-item admin-list-group-item">
                                <span><%= user.jmeno %> (<%= user.email %>)</span>
                                <form action="/authorizeUser" method="post" style="display:inline;">
                                    <input type="hidden" name="userId" value="<%= user.id %>">
                                    <button type="submit" class="btn admin-btn admin-btn-authorize">
                                        <i class="bi bi-check-circle"></i> Potvrdit
                                    </button>
                                </form>
                            </li>
                        <% }) %>
                    </ul>
                </div>
            </div>
        </div>
    </main>
    <!-- Event Overlay -->
    <div id="eventOverlay" class="overlay">
        <div class="overlay-content">
            <span class="closebtn" onclick="closeEventOverlay()">&times;</span>
            <h2>Vyberte akci k úpravě</h2>
            <ul class="event-list">
                <% events.forEach(event => { %>
                    <li>
                        <form action="/adminEditAkce" method="get">
                            <input type="hidden" name="eventTitle" value="<%= event.title %>">
                            <button type="submit" class="event-item">
                                <span><%= event.title %></span>
                                <span><%= new Date(event.start).toLocaleString('cs-CZ', { year: 'numeric', month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit' }) %></span>
                            </button>
                        </form>
                    </li>
                <% }) %>
            </ul>
        </div>
    </div>
    <script>
        function openNav() {
            document.getElementById("sidebar").style.width = "250px";
        }
        function closeNav() {
            document.getElementById("sidebar").style.width = "0";
        }
        function openEventOverlay() {
            document.getElementById("eventOverlay").style.display = "block";
        }
        function closeEventOverlay() {
            document.getElementById("eventOverlay").style.display = "none";
        }
    </script>
</body>
</html>